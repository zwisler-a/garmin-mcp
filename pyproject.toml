[project]
name = "garmin-mcp"
version = "0.1.0"
description = "MCP server for fetching data from Garmin Connect"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.28.1",
    "garminconnect>=0.3.6",
    "python-dotenv>=1.2.2",
]
